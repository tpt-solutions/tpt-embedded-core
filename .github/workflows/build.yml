name: Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build (${{ matrix.chip }})
    strategy:
      matrix:
        include:
          - chip: esp32
            target: xtensa-esp32-none-elf
          - chip: esp32s3
            target: xtensa-esp32s3-none-elf
          - chip: esp32c3
            target: riscv32imc-unknown-none-elf
          - chip: esp32c6
            target: riscv32imac-unknown-none-elf
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: |
            xtensa-esp32-none-elf
            xtensa-esp32s3-none-elf
            riscv32imc-unknown-none-elf
            riscv32imac-unknown-none-elf
      - uses: esp-rs/xtensa-toolchain@v1
        with:
          ldproxy: true
      # Each matrix leg now actually builds for its own chip: the target
      # triple and the esp-hal chip feature both vary per leg (previously
      # this ran the identical host-target command 4 times).
      - run: cargo build -p tpt-e-typestate-hal --target ${{ matrix.target }} --features "use_esp_hal,${{ matrix.chip }}"
      - run: cargo build -p tpt-e-slumber --target ${{ matrix.target }} --features "use_esp_hal,${{ matrix.chip }}"
      # tpt-e-chronos/tpt-e-cipher/tpt-e-swarm-sync have no use_esp_hal/chip
      # features yet (no real hardware backend), so these build with default
      # features — but that alone is worth catching in CI: a 2026-07-30 audit
      # found tpt-e-chronos failed to even *compile* for riscv32imc
      # (ESP32-C3) because these three crates were never cross-compiled to a
      # real chip target here before, only ever built host-side (`--workspace
      # --features mock` below, or `build-default-features`, both host/x86_64).
      - run: cargo build -p tpt-e-chronos --target ${{ matrix.target }}
      - run: cargo build -p tpt-e-cipher --target ${{ matrix.target }}
      - run: cargo build -p tpt-e-swarm-sync --target ${{ matrix.target }}
      - run: cargo build --workspace --features mock

  build-default-features:
    name: Build (default features, host)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      # No --features here on purpose: this is the regression guard for the
      # 2026-07-22 fix where tpt-e-typestate-hal and tpt-e-swarm-sync both
      # failed to compile with no features enabled, and CI never caught it
      # because every other job always passes --features mock.
      - run: cargo build --workspace

  deny:
    name: License & dependency audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check
